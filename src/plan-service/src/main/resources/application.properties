spring.application.name=plan-service

# Local Development Database (Docker PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/planbookai
spring.datasource.username=caesar
spring.datasource.password=290304
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8081

# Actuator Configuration for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized